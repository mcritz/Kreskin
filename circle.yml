dependencies:
  override:
    - eval "$(curl -sL https://apt.vapor.sh)"
    - sudo apt-get install vapor=2.4.4
    - sudo chmod -R a+rx /usr/
test:
  override:
    - swift build
    - swift build -c release
    - swift test
    